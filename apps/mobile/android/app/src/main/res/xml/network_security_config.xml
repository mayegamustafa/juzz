<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic must be HTTPS. Cleartext is permitted only for the local
  development API: 10.0.2.2 is the Android emulator's alias for the host machine.

  To test against a LAN address on a physical device, add that host below rather
  than enabling cleartext globally.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
